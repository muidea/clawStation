# 当前待办与状态

更新时间：`2026-03-26`

## 1. 当前目标

当前主线已收口为：

1. 把“实例添加完成即可投入使用”的向导链路稳定下来。
2. 把自动认证匹配整理成可复用设计文档。
3. 对当前通过验证的实现做归档，作为后续增量开发基线。

---

## 2. 当前结论

### 2.1 已确认

1. `edge` 的 SSH、隧道、聊天、历史记录均可正常工作。
2. `1058` 的 SSH、隧道、HTTP 探测、自动认证匹配现已打通。
3. 向导内 `连接验证` 现在会顺序完成：
   - SSH
   - OpenClaw 探测
   - shared token 发现
   - 自动 pairing
   - Gateway 重试握手
4. 容器部署和宿主机部署的控制面分流已生效。
5. 自动认证匹配的设计文档已落到：
   - `docs/自动认证匹配设计.md`

### 2.2 当前判断

1. 当前 P0 阻塞已经从“认证匹配不可用”转为“收尾、归档和后续复用”。
2. 后续若再出现同类问题，应优先按 `transport / shared_token / device / gateway` 四层定位。
3. 向导验证链路和聊天链路必须继续保持一致，不能再分叉出“只握手一次”的简化实现。

---

## 3. 实例状态

| 目标 | 部署方式 | 当前状态 | 结论 |
|------|------|------|------|
| `edge` | Docker 容器 `work-station` | 已可正常聊天/历史 | 自动发现 + 自动 pairing 已验证通过 |
| `1058` | 宿主机本地部署 | 已通过向导验证 | shell/profile/PATH 修复 + 自动 pairing 已验证通过 |

---

## 4. 待办

### 4.1 P0

1. 归档当前通过验证的桌面端与 `clawops` 改动。
2. 保留并同步自动认证匹配设计文档。

### 4.2 P1

1. 继续补向导交互测试和回归测试，覆盖：
   - 容器部署
   - 宿主机部署
   - shared token 发现
   - 自动 pairing
2. 继续收口向导和详情页文案，减少历史概念残留。

### 4.3 P2

1. 视需要再把这套自动认证匹配能力抽成更独立的共享模块。
2. 再考虑是否补手工 Gateway token 入口作为兜底。

---

## 5. 当前不做

1. 不继续在桌面端盲改聊天层逻辑。
2. 不把 WebUI 手工 approve 当成默认方案。
3. 不把容器部署与宿主机部署混成一条控制面策略。

---

## 5.1 已落地的实例模型调整

`2026-03-25` 已补第一版“部署信息”建模，并接入桌面端与网关控制面：

1. 实例记录新增：
   - `targetPlatform`
   - `deploymentMode`
   - `controlPlaneMode`
   - `containerName`
2. 新建托管实例向导已新增“部署信息”步骤：
   - 可区分 `container` 与 `host_process`
   - 宿主机场景不再强制要求部署模板
3. `gateway.discover` / `gateway.authorizeDevice` 已优先使用实例上的显式控制面信息：
   - `container` 优先走 `docker_exec`
   - `host_process` 优先走 `host_cli`

当前这部分仍未完成的点：

1. `clawops` 的 deploy/probe/restart/rollback 仍主要围绕容器部署设计。
2. 宿主机托管实例目前重点覆盖聊天链路的 Gateway 控制面，不等于完整的一键部署/运维托管。
3. 下一阶段仍要把 `clawcli` 的显式 token 验证结论回落到桌面端 token 自动发现策略。

---

## 5.2 `host` 模型收口进度

`2026-03-25` 已完成第一轮“一步到位去 host 化”：

1. `clawconsole`：
   - 新建托管实例向导不再调用 `host.list/create/get`
   - “主机连接信息”直接写入实例
   - 实例模型新增并落盘 `deployRoot`
2. `clawops`：
   - 删除 `host.*` RPC
   - 删除 `HostsStore / HostProfile / host_profile_id`
   - `InstanceDeployment` 改为直接持有：
     - `host`
     - `sshPort`
     - `credentialId`
     - `deployRoot`
   - deploy / probe / logs / lifecycle / gateway 控制面统一改为直接从 deployment 取 SSH 与部署信息
3. CLI：
   - `precheck` 已不再接受 `hostId`

当前这条线剩余确认项：

1. 用新的 Windows 包再验证一次托管实例向导与实例编辑页。
2. 确认 `edge / 1058` 的 Gateway 控制面日志仍能按 `deploymentMode` 正确分流。

---

## 5.3 Gateway 宿主机执行环境修复

`2026-03-26` 已确认并修复一条真实问题：

1. `1058` 宿主机上手工执行 `openclaw dashboard --no-open` 可以成功返回带 token 的 URL。
2. 桌面端自动发现失败的真实根因不是 token 不存在，而是 SSH 非交互执行环境缺少 `node`：
   - 日志中可见 `/usr/bin/env: ‘node’: No such file or directory`
3. `clawops` 现已补上：
   - 远端登录 shell 探测
   - 按 `bash / zsh / sh` 分流命令包装
   - profile 加载
   - 常见 `PATH` 与 `nvm/node` 路径注入
4. `gateway.discover` 与 `gateway.authorizeDevice` 的原始远端命令结果也已直接写入 `runtime.log`，用于后续继续排障。

当前状态：

1. `1058` 上的 Gateway 自动认证匹配已验证通过。
2. 当前不再优先排查 shared token 获取本身，而是继续把验证时机前移到实例创建向导。

---

## 5.4 向导内验证改造进度

`2026-03-26` 已完成第一版“向导内验证并完成”实现：

1. 新建实例向导新增最终步骤：
   - `连接验证`
2. “已有实例接入”路径现在会在向导内顺序执行：
   - SSH 校验
   - OpenClaw 探测
   - Gateway 自动认证匹配
3. “新实例部署”路径现在会在向导内顺序执行：
   - 部署 operation 轮询
   - SSH / OpenClaw 可用性确认
   - Gateway 自动认证匹配
4. 校验失败后支持：
   - 保存草稿
   - 已有实例接入路径返回修改
5. 校验状态已从旧的 `adopted/access_only` 语义放宽为：
   - 任何实例只要带 `pending/failed` 校验状态，就会被阻断连接/打开/聊天

当前已完成验证：

1. `npm run build` 通过
2. `npm test` 通过
3. `cargo test -q`（`clawconsole/src-tauri`）通过
4. `cargo test -q`（`clawops`）通过

当前剩余事项：

1. 用新的 Windows 包验证向导内验证流程与失败分支交互。
2. 视验证结果，决定是否继续把“新实例部署失败后的返回修改”做成更彻底的资源清理流程。

---

## 6. 下一步操作建议

### 6.1 `edge`

用于确认基线：

```bash
go run ./cmd/clawcli token \
  -device-id edge-auto-verify \
  -ssh-target rangh@edge.mulife.vip \
  -ssh-port 1212 \
  -docker-container work-station
```

```bash
go run ./cmd/clawcli chat \
  -device-id edge-auto-verify \
  -ssh-target rangh@edge.mulife.vip \
  -ssh-port 1212 \
  -docker-container work-station \
  -message "hello"
```

### 6.2 `1058`

先做显式 token 基准验证：

```bash
CLAWCLI_GATEWAY_TOKEN='<shared-gateway-token>' \
go run ./cmd/clawcli token \
  -device-id host-1058-verify \
  -ssh-target develop@192.168.10.58 \
  -ssh-port 22
```

```bash
CLAWCLI_GATEWAY_TOKEN='<shared-gateway-token>' \
go run ./cmd/clawcli chat \
  -device-id host-1058-verify \
  -ssh-target develop@192.168.10.58 \
  -ssh-port 22 \
  -message "hello"
```

---

## 7. 恢复进度时先看什么

下次恢复工作时，先按这个顺序读：

1. 本文件
2. `clawcli/设计文档.md`
3. `clawcli/CLI使用说明.md`
4. 最新一次 `AUTH_TOKEN_MISSING` 的日志与验证记录
