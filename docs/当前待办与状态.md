# 当前待办与状态

更新时间：`2026-03-26`

## 1. 当前目标

当前主线已收口为两项：

1. 继续完善实例添加向导与实例详情的产品体验。
2. 在已验证通过的自动认证匹配基础上，补测试、补收尾、控回归。

---

## 2. 当前结论

### 2.1 已完成并验证通过

1. `edge` 的 SSH、隧道、聊天、历史记录均可正常工作。
2. `1058` 的 SSH、隧道、HTTP 探测、自动认证匹配现已打通。
3. 向导内 `连接验证` 已能顺序完成：
   - SSH
   - OpenClaw 探测
   - shared token 发现
   - 自动 pairing
   - Gateway 重试握手
4. 容器部署和宿主机部署的控制面分流已生效。
5. 自动认证匹配设计文档已落到：
   - `docs/自动认证匹配设计.md`
6. 当前已完成 git 归档，后续开发以当前提交点为基线。

### 2.2 当前判断

1. 当前 P0 阻塞已经从“认证匹配不可用”转为“体验收尾、测试补强和回归控制”。
2. 后续若再出现同类问题，应优先按 `transport / shared_token / device / gateway` 四层定位。
3. 向导验证链路和聊天链路必须继续保持一致，不能再分叉出“只握手一次”的简化实现。

---

## 3. 实例状态

| 目标 | 部署方式 | 当前状态 | 结论 |
|------|------|------|------|
| `edge` | Docker 容器 `work-station` | 已可正常聊天/历史 | 自动发现 + 自动 pairing 已验证通过 |
| `1058` | 宿主机本地部署 | 已通过向导验证 | shell/profile/PATH 修复 + 自动 pairing 已验证通过 |

---

## 4. 待办

### 4.1 P0

1. 清理并收口向导、实例详情、状态提示中的残留历史概念和冗余文案。
2. 核对“重新校验”“保存草稿”“返回修改”等分支交互，避免再出现向导内外逻辑不一致。
3. 补一轮针对实例添加/验证链路的前后端回归测试用例清单。

### 4.2 P1

1. 继续补向导交互测试和回归测试，覆盖：
   - 容器部署
   - 宿主机部署
   - shared token 发现
   - 自动 pairing
2. 评估是否需要把“自动认证匹配”进一步抽成更独立的共享模块，减少向导验证与聊天链路重复逻辑。

### 4.3 P2

1. 视需要补手工 Gateway token 入口作为兜底能力，但当前不作为默认路径。
2. 视需要再扩展到更完整的宿主机运维托管能力。

---

## 5. 当前不做

1. 不继续在桌面端盲改聊天层逻辑。
2. 不把 WebUI 手工 approve 当成默认方案。
3. 不把容器部署与宿主机部署混成一条控制面策略。

---

## 6. 参考文档

1. `docs/自动认证匹配设计.md`
2. `docs/设计文档.md`
3. `docs/产品定义.md`
4. `docs/实例托管联调验证.md`

---

## 7. 恢复进度时先看什么

下次恢复工作时，先按这个顺序读：

1. 本文件
2. `docs/自动认证匹配设计.md`
3. 最新一次实例添加/连接验证相关日志
4. 再决定是继续做体验收尾，还是做回归测试补强
